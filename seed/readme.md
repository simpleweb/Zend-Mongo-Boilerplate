Use mongo import for seed data:

http://www.mongodb.org/display/DOCS/Import+Export+Tools#ImportExportTools-mongoimport

Can call ./load.sh (might need to chmod +x load.sh) in order to load all seed data.
